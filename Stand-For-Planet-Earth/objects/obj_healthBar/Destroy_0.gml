/// @description 
with(obj_healthBar_vignette){
    if (self.hero == other.hero){
        instance_destroy ();
    }
}
with(obj_electricityFX){
    if (self.hero == other.hero){
        instance_destroy ();
    }
}