///scr_clearAllBinds
with(obj_localPlayer)
{
    keyboard_clear (bindRight);
    keyboard_clear (bindLeft);
    keyboard_clear (bindUp);
    keyboard_clear (bindDown);
    keyboard_clear (bindJump);
    keyboard_clear (bindDash)
    mouse_clear(bindFire1);
    mouse_clear (bindFire2);
    keyboard_clear (bindUse);
}
