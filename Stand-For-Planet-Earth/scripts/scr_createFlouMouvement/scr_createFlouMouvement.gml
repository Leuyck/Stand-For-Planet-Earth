///scr_createFlouMouvement(image_color,duration)

if (state="dashing"|| bump)
{
	scr_motionBlur(500,direction+180)
/*
	
    var flouMouvement = instance_create(x,y,obj_flouMouvement);
        flouMouvement.sprite_index = sprite_index;
        flouMouvement.image_index = image_index;
        flouMouvement.image_angle = image_angle;
        flouMouvement.depth = depth;
        flouMouvement.fade_color = argument[0];
        flouMouvement.fade_steps = argument[1]*room_speed;*/
}
